# Substrate Agent Requirements

# Core dependencies
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.25.0
pyyaml>=6.0

# GPU acceleration (optional)
bitsandbytes>=0.41.0

# Development dependencies
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
